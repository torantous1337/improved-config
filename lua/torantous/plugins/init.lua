-- ============================================================================
--  PLUGIN LIST (lazy.nvim auto-loads all files in this folder)
-- ============================================================================
--  NOTE: When using require("lazy").setup("torantous.plugins", ...) in
--  lua/torantous/init.lua, lazy.nvim automatically loads every .lua file
--  in this directory as a plugin spec. This init.lua must NOT re-import
--  the same folder, or it creates a redundant/recursive import cycle.
-- ============================================================================

return {}
